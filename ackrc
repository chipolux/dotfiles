# Add type for powershell files
--type-set=ps:ext:ps1,psm1,psd1

# Update python type to include pyw files
--type-add=python:ext:pyw

# Ignore js non-source dirs
--ignore-dir={bin,node_modules,vendor,app-components}

# Use less as pager
--pager=less -FX

# Always sort files
--sort-files
